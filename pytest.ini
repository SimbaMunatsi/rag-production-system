[pytest]
asyncio_mode = auto
markers =
    eval: marks tests as LLM evaluations (deselect with '-m "not eval"')
filterwarnings =
    # Ignore internal deprecation warnings generated by the Ragas library itself
    ignore::DeprecationWarning:ragas.*